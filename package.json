{
  "name": "revers-messenger",
  "version": "1.0.0",
  "description": "P2P мессенджер с полной анонимностью",
  "scripts": {
    "build": "npx esbuild src/app.js --bundle --outfile=src/bundle.js --format=iife",
    "apk": "npm run build && npx cap sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "simple-peer": "^9.11.1"
  },
  "devDependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/assets": "^3.0.0",
    "esbuild": "^0.19.0"
  }
}
