{
  "name": "revers-messenger",
  "version": "1.0.0",
  "description": "P2P мессенджер с полной анонимностью",
  "main": "src/index.html",
  "scripts": {
    "dev": "npx cap sync && npx cap open android",
    "build": "npx cap sync && cd android && ./gradlew assembleDebug",
    "sync": "npx cap sync",
    "icons": "npx capacitor-assets generate"
  },
  "dependencies": {
    "hyperswarm": "^4.0.0",
    "simple-peer": "^9.11.1",
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1",
    "b4a": "^1.6.0",
    "compact-encoding": "^2.12.0"
  },
  "devDependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/assets": "^3.0.0"
  }
}
