{
  "name": "revers-messenger",
  "version": "3.3.0",
  "scripts": {
    "build": "npx esbuild src/app.js --bundle --outfile=src/bundle.js --format=iife --platform=browser",
    "apk": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "qrcode": "^1.5.3",
    "html5-qrcode": "^2.3.8",
    "@capacitor/browser": "^5.0.0",
    "libsodium-wrappers-sumo": "^0.7.13",
    "@noble/post-quantum": "^0.3.0",
    "pako": "^2.1.0"
  },
  "devDependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/assets": "^3.0.0",
    "esbuild": "^0.19.0"
  }
}
